name: Images

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write

concurrency:
  group: images-${{ github.ref }}
  cancel-in-progress: false

jobs:
  images:
    uses: tesserix/tesserix-workflows/.github/workflows/vehicle-rental-images.yml@v1.0.0
